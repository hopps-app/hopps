import type { MenuItem } from './types';

/**
 * Intentionally empty — the admin nav has no destinations yet.
 * The sidebar renders an empty-state hint in its place; add MenuItems here
 * and it will render them without further changes.
 */
export const menuConfig: MenuItem[] = [];
