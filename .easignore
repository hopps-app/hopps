# Ignore everything by default
**

# Allow the mobile directory
!frontend/mobile
!frontend/mobile/**

# Optional: still ignore any hidden files in the repo root, except those in frontend/mobile
.*
!frontend/mobile/.*

node_modules
