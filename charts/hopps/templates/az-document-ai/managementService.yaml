apiVersion: v1
kind: Service
metadata:
  name: {{ include "hopps.azDocumentAiFullname" . }}-management
  labels:
    {{- include "hopps.azDocumentAiLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 9000
      targetPort: management
      protocol: TCP
      name: management
  selector:
    {{- include "hopps.azDocumentAiSelectorLabels" . | nindent 4 }}
