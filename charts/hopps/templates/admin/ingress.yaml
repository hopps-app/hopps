{{- if .Values.admin.ingress.enabled -}}
{{- $fullName := include "hopps.adminFullname" . -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "hopps.adminLabels" . | nindent 4 }}
{{- with .Values.admin.ingress.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
spec:
  {{- if or (.Capabilities.APIVersions.Has "networking.k8s.io/v1/IngressClass") (.Capabilities.APIVersions.Has "networking.k8s.io/v1beta1/IngressClass") }}
  {{- if .Values.admin.ingress.ingressClassName }}
  ingressClassName: {{ .Values.admin.ingress.ingressClassName }}
  {{- end }}
  {{- end }}
{{- if .Values.admin.ingress.tls }}
  tls:
  {{- range .Values.admin.ingress.tls }}
    - hosts:
      {{- range .hosts }}
        - {{ . | quote }}
      {{- end }}
      secretName: {{ .secretName }}
  {{- end }}
{{- end }}
  rules:
  {{- range .Values.admin.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - pathType: {{ .pathType }}
            path: {{ .path }}
            backend:
              service:
                name: {{ $fullName }}
                port:
                  name: http
          {{- end }}
  {{- end }}
{{- end }}
