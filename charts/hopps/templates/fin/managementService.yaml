apiVersion: v1
kind: Service
metadata:
  name: {{ include "hopps.finFullname" . }}-management
  labels:
    {{- include "hopps.finLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 9000
      targetPort: management
      protocol: TCP
      name: management
  selector:
    {{- include "hopps.finSelectorLabels" . | nindent 4 }}
