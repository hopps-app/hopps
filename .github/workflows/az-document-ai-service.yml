name: Build az-document-ai-service

on:
  push:
    branches: [ main, "2.0" ]
    paths:
      - 'app.hopps.az-document-ai/**'
      - '.github/workflows/az-document-ai-service.yml'
  workflow_dispatch:

defaults:
  run:
    working-directory: app.hopps.az-document-ai

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: 'maven'

      - name: Build with Maven
        run: ./mvnw -B verify

      - name: Cache SonarQube packages
        uses: actions/cache@v4
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      - name: SonarQube analysis
        if: ${{ !startsWith(github.ref, 'refs/heads/dependabot') }}
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          ./mvnw -B sonar:sonar \
            -Dsonar.host.url=https://sonarcloud.io \
            -Dsonar.organization=hopps-app \
            -Dsonar.projectKey=hopps-app_hopps-document-ai

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        if: ${{ success() }}
        uses: docker/build-push-action@v6
        with:
          context: app.hopps.az-document-ai
          push: true
          file: app.hopps.az-document-ai/src/main/docker/Dockerfile.jvm
          tags: ghcr.io/hopps-app/az-document-ai-service:latest
