name: Build zugferd-service

on:
  push:
    branches: [ main, deploy ]
    paths:
      - 'app.hopps.zugferd/**'
      - '.github/workflows/zugferd-service.yml'
  workflow_dispatch:

defaults:
  run:
    working-directory: app.hopps.zugferd

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: 'maven'

      - name: Build with Maven
        run: ./mvnw -B verify

      - name: Cache SonarQube packages
        uses: actions/cache@v4
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      - name: SonarQube analysis
        if: ${{ !startsWith(github.ref, 'refs/heads/dependabot') }}
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          ./mvnw -B sonar:sonar \
            -Dsonar.host.url=https://sonarcloud.io \
            -Dsonar.organization=hopps-app \
            -Dsonar.projectKey=hopps-app_zugferd

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        if: ${{ success() }}
        uses: docker/build-push-action@v6
        with:
          context: app.hopps.zugferd
          push: true
          file: app.hopps.zugferd/src/main/docker/Dockerfile.jvm
          tags: |
            ghcr.io/hopps-app/zugferd-service:latest
            ghcr.io/hopps-app/zugferd-service:${{ github.run_number }}

      - name: Trigger deploy workflow
        uses: actions/github-script@v7
        if: ${{ github.ref == 'refs/heads/main' }}
        with:
          github-token: ${{ secrets.TECHNICAL_USER_PAT }}
          script: |
            const result = await github.rest.actions.createWorkflowDispatch({
              owner: 'hopps-app',
              repo: 'hopps.cloud',
              workflow_id: 'deploy.yml',
              ref: 'main',
              inputs: {
                "environment": "dev",
                "new-tag": "${{ github.run_number }}",
                "service-name": "zugferd"
              }
            })
            console.log(result);
